# Core Dependencies
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
gradio>=4.0.0

# Optional but recommended
accelerate>=0.24.0
bitsandbytes>=0.41.0  # For 4-bit quantization (requires CUDA)

# Utilities
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# Alternative for CPU-only environments (uncomment if needed)
# Use faiss-cpu instead of faiss-gpu for CPU-only systems
